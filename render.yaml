services:
  - type: web
    name: portfolio
    runtime: node
    buildCommand: npm install --legacy-peer-deps && npm run build
    startCommand: npx serve dist
    envVars:
      - key: NODE_VERSION
        value: "20"
